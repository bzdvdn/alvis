[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "alvis-kb-catalog"
version = "0.1.0"
description = "Catalog source plugin for Alvis (Plugin SDK example)"

[project.entry-points."alvis.plugins"]
kb-catalog = "kb_plugin:plugin"

[tool.hatch.build.targets.wheel]
packages = ["kb_plugin"]