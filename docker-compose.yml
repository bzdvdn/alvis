name: winnow-dev

services:
  qdrant:
    image: qdrant/qdrant:v1.10.0
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage

  mock-confluence:
    build: ./docker/mock-confluence
    ports:
      - "8090:8090"

volumes:
  qdrant_storage: