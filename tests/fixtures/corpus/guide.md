# Onboarding Guide

Alvis ingests documents from many sources and turns them into a searchable
corpus.

## First run

Write a pipeline file, then run `alvis run pipeline.yaml`.

## Health check

`alvis validate pipeline.yaml` checks the config before running.
