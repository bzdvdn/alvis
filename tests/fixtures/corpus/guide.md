# Onboarding Guide

Winnow ingests documents from many sources and turns them into a searchable
corpus.

## First run

Write a pipeline file, then run `winnow run pipeline.yaml`.

## Health check

`winnow validate pipeline.yaml` checks the config before running.
